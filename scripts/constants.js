/**
 * The module's canonical identifier — mirrors the `id` field in module.json.
 * Import this constant everywhere the module ID is needed instead of
 * writing the string literal, so a future rename is a single-file change.
 */
export const MODULE_ID = "dh-mystery-box";
